# รับค่าอินพุตจากผู้ใช้
n = int(input())

# เริ่มต้นจำนวนลูกดอกที่ต้องใช้
count = 0

# ลูปผ่านแต้มที่สามารถปาได้จากมากไปน้อย
for score in [5, 4, 3, 2, 1]:
    if n == 0:
        break
    count += n // score
    n %= score

# แสดงจำนวนลูกดอกที่น้อยที่สุดที่ต้องใช้
print(count)
